# STALE — this file is kept as a placeholder only.
# Dev dependencies are defined in trading_studio/pyproject.toml [project.optional-dependencies]
# Install with: pip install -e "./trading_studio[dev]"
